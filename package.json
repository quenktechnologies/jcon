{
  "name": "@quenk/jcon",
  "version": "0.8.0",
  "description": "JavaScript Configuration Object Notation",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "clean": "rm -R lib/*",
    "build:parser": "./node_modules/.bin/jison -o src/Parser.js src/jcon.y && cp src/Parser.js lib/Parser.js",
    "build:src": "./node_modules/.bin/tsc -p src",
    "build": "npm run -s build:parser && npm run -s build:src",
    "test": "./node_modules/.bin/mocha --opts mocha.opts test/parse.ts"
  },
  "author": "Quenk Technologies Limited <dev@quenk.com> (http://quenk.com)",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "@types/node": "^7.10.2",
    "jison-gho": "^0.6.1-215",
    "mocha": "^3.5.3",
    "must": "^0.13.4",
    "ts-node": "^3.0.4",
    "typescript": "^2.9.2"
  }
}
